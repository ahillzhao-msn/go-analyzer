# KataGo analysis config — Go Analyzer v0.4.0
# 日志由 Worker 的 Python logging 管理
# 注释掉 logDir 让 KataGo 使用默认行为（不写日志文件）
# logDir is intentionally omitted — KataGo writes to stdout which Python captures

reportAnalysisWinratesAs = BLACK
analysisPVLen = 15
wideRootNoise = 0.04
numAnalysisThreads = 8
numSearchThreads = 15
nnMaxBatchSize = 90
